# yaml-language-server: $schema=none
apiVersion: 1

providers:
  - name: Banking Peak Load
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
