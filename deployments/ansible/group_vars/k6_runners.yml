---
# deployments/ansible/group_vars/k6_runners.yml

repo_url: "https://github.com/marquisccel/banking-peak-load-prototype"
app_dir: "/home/ubuntu/banking-peak-load-prototype"

# app_base_url is set per-host from the Terraform inventory
# (http://<app_public_ip>:8080)
